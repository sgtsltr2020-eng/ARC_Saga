name: ARC SAGA CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio httpx

      - name: Run Black formatting check
        run: black --check arc_saga tests

      - name: Run isort import check
        run: isort --check-only --profile black arc_saga tests

      - name: Run Ruff linting
        run: ruff check arc_saga tests

      - name: Run mypy type checking
        run: mypy arc_saga tests
        continue-on-error: true

      - name: Run pylint
        run: pylint arc_saga tests --fail-under=8.0
        continue-on-error: true

      - name: Run Bandit security scan
        run: bandit -r arc_saga -x tests --severity-level=medium -f json -o bandit-report.json || true

      - name: Run tests with coverage
        run: |
          pytest --cov=arc_saga --cov-report=xml --cov-report=term-missing tests/

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

      - name: Check for TODO/FIXME in production code
        run: |
          if grep -r "TODO\|FIXME" arc_saga/ --exclude-dir=__pycache__; then
            echo "WARNING: Found TODO/FIXME in production code"
          fi
        continue-on-error: true

  benchmark:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-benchmark

      - name: Run performance benchmarks
        run: pytest tests/ -m benchmark --benchmark-only || echo "No benchmarks configured yet"
        continue-on-error: true